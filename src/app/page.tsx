import CTA from "@/components/CTA/CTA";

export default function Home() {
  return (
    <main
      style={{
        padding: "5rem",
        background: "#0b1c2d",
        minHeight: "100vh",
      }}
    >
      <CTA />
    </main>
  );
}